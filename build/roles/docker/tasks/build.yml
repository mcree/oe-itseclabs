---

- name: copy docker image recipes to /tmp
  copy:
    src: ./build
    dest: /tmp/

- name: Build local docker images
  docker_image:
    name: "{{ item }}"
    build:
      path: "/tmp/{{ item }}"
  with_items:
    - xca